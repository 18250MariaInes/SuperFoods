<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.superfoods">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".TodosMisProductos"
        android:label="Mis Productos">
        </activity>
        <activity android:name=".MostrarProducto"
                  android:label="Mostar Producto">
        </activity>
        <activity android:name=".CrearProducto"
                  android:label="Producto Nuevo">
        </activity>
        <activity android:name=".BuscarProductos"
                  android:label="Buscar Productos">
        </activity>
        <activity android:name=".TodasRecetas"
                  android:label="Todas las Recetas">
        </activity>
        <activity android:name=".MostrarReceta"
                  android:label="Mostrar Receta">
        </activity>
        <activity android:name=".GuardarReceta"
                  android:label="Receta Nueva"
        android:windowSoftInputMode="adjustResize|stateHidden">

        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
                  android:label="Superfoods">
        </activity>
        <activity android:name=".RegisterActivity"
                  android:label="Crea una cuenta">
        </activity>
        <activity android:name=".ForgotPassActivity"
                  android:label="Recuperacion de Contraseña">
        </activity>
        <activity android:name=".ShowProfileActivity"
                  android:label="Tu perfil">
        </activity>
    </application>

</manifest>